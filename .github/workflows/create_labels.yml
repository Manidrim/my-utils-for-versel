name: Create Required Labels

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - '.github/workflows/*.yml'

jobs:
  call:
    uses: ./.github/workflows/reusable-create-labels.yml
